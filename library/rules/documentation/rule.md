# Documentation

- Update `README.md` when setup, env vars, scripts, or prerequisites change.
- Document non-obvious architecture decisions in `docs/` or ADRs if the project uses them.
- Keep public API docs and examples in sync with code changes.
- Prefer short, task-oriented docs over long narrative when onboarding is not the goal.
