---
name: eslint-prettier-skill
description: Resolve lint/format issues using the repo's ESLint and Prettier setup
---

# ESLint / Prettier skill

When lint fails:

1. Fix the underlying issue before suppressing.
2. If rules conflict, propose config changes rather than inconsistent code style.

Use after refactors or large file moves.
