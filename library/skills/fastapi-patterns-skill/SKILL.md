---
name: fastapi-patterns-skill
description: Add FastAPI endpoints with dependencies, Pydantic models, and clear errors
---

# FastAPI skill

When building APIs:

1. Model inputs/outputs explicitly.
2. Wire cross-cutting concerns through `Depends`.
3. Match existing error and logging patterns.

Use for CRUD routes, auth middleware, or OpenAPI documentation updates.
