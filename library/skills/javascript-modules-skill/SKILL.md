---
name: javascript-modules-skill
description: Structure JS packages with clear ESM/CJS boundaries and minimal side effects
---

# JavaScript modules skill

When adding or reorganizing JS files:

1. Match existing `import`/`export` or `require` style.
2. Avoid circular dependencies; extract shared utilities if needed.
3. Document complex object shapes with JSDoc when TS is not in use.
